import { View, Text } from "react-native";

const Main = () => {
  return (
    <View className="flex-1 items-center justify-center bg-primary">
      <Text className="text-2xl font-bold text-secondary">Mypage</Text>
    </View>
  );
};

export default Main;
